{
  "framework": "vite",
  "buildCommand": "cd _bmad-output/frontend && npm install && npm run build",
  "outputDirectory": "_bmad-output/frontend/dist",
  "installCommand": "echo skip",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
